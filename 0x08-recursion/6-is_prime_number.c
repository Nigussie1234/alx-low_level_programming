#include <stdio.h>
#include "holberton.h"

int main()
{
int i, n;
i = n / 2; 
/**
 *is_prime_number - returns 1 if the input integer is a prime number
 *@n: integer
 *
 *Return: 1 or 0
 */

int is_prime_number(int n)
{
  if (i == 1)
    {
      return (1);
    }
  
